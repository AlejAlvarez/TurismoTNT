import React from 'react';

// createContext() no toma valor inicial. Toma el valor que será devuelto si intentamos
// utilizar este contexto sin un Provider.
export default UserContext = React.createContext(null);